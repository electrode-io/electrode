import React from "react";

/**
 * The React context for the app context feature.
 *
 * @remark See {@link appContextFeature} for example and usage
 */
export const AppContext = React.createContext<any>({});
