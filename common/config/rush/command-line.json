{
  "$schema": "https://developer.microsoft.com/json-schemas/rush/v5/command-line.schema.json",
  "commands": [
    {
      "commandKind": "bulk",
      "name": "test",
      "summary": "Run tests in each packages",
      "description": "Run tests in each packages. Runs `npm run test` for each package. Each package can have its own definition of `npm run test`",
      "allowWarningsInSuccessfulBuild": true,
      "ignoreDependencyOrder": false,
      "ignoreMissingScript": true,
      "enableParallelism": false
    },
    {
      "commandKind": "global",
      "name": "find-deps",
      "summary": "Find all references to a dependency package in the mono repo",
      "description": "Searches through all packages in the repository to find which ones depend on a specified package. Usage: rush find-deps --package <package-name>",
      "safeForSimultaneousRushProcesses": true,
      "shellCommand": "node common/scripts/find-deps.js"
    },
    {
      "commandKind": "bulk",
      "name": "lint",
      "summary": "Run lint in each packages",
      "description": "Run lint in each packages. Runs `npm run lint` for each package if present.",
      "allowWarningsInSuccessfulBuild": true,
      "ignoreMissingScript": true,
      "enableParallelism": false
    },
    {
      "name": "commitlint",
      "commandKind": "global",
      "summary": "Used by the pre-commit Git hook. This command invokes commitlint to ensure that the commit messages meet the conventional commit format",
      "safeForSimultaneousRushProcesses": true,
      "autoinstallerName": "commitlint",
      "shellCommand": "commitlint"
    },
    {
      "commandKind": "bulk",
      "name": "build",
      "summary": "Build all projects that haven't been built, or have changed since they were last built",
      "description": "This command is similar to \"rush rebuild\", except that \"rush build\" performs an incremental build",
      "safeForSimultaneousRushProcesses": false,
      "enableParallelism": true,
      "ignoreDependencyOrder": false,
      "ignoreMissingScript": false,
      "allowWarningsInSuccessfulBuild": true
    }
  ],
  "parameters": [
    {
      "parameterKind": "string",
      "argumentName": "MESSAGE",
      "longName": "--edit",
      "description": "Command for commitlint",
      "associatedCommands": [
        "commitlint"
      ]
    },
    {
      "parameterKind": "string",
      "argumentName": "PACKAGE",
      "longName": "--package",
      "description": "Command for find-deps",
      "associatedCommands": [
        "find-deps"
      ]
    },
    {
      "parameterKind": "flag",
      "longName": "--no-node-modules",
      "description": "Exclude dependencies found in node_modules directories from the results",
      "associatedCommands": [
        "find-deps"
      ]
    },
    {
      "parameterKind": "flag",
      "longName": "--direct-only",
      "description": "Only show direct dependencies, excluding transitive dependencies in the results",
      "associatedCommands": [
        "find-deps"
      ]
    }
  ]
}
